---
description: 작업 마무리 시 빌드 검증 필수
globs: **/*.ts,**/*.tsx
---

# 빌드 검증 규칙

작업을 마무리하기 전에 반드시 `npx next build`를 실행하여 빌드가 성공하는지 확인한다.

- TypeScript 타입 에러가 없는지 확인
- 타입 정의 파일(인터페이스, type 등)을 수정했거나 새 프로퍼티를 사용하는 경우, 해당 타입 정의가 함께 업데이트되었는지 반드시 확인
- 빌드 실패 시 원인을 해결한 뒤에만 작업 완료로 간주
